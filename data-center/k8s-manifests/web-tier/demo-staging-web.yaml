apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-staging-web
  labels:
    app: demo-staging-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo-staging-web
  template:
    metadata:
      labels:
        app: demo-staging-web
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: demo-staging-web
spec:
  selector:
    app: demo-staging-web
  ports:
    - port: 80
      targetPort: 80
